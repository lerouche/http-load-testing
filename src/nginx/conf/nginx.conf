worker_processes auto;

events {
    worker_connections 1000000;
}

http {

    access_log off;
    error_log logs/nginx-error.log;

    lua_package_cpath "lualib/?.so;;";
    lua_package_path "lualib/?.lua;site/lualib/?.lua;;";

    include nginx-resty.conf;
    #include nginx-hhvm.conf;
}
